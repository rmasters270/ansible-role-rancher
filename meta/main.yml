galaxy_info:
  author: rmasters270
  description: Install Rancher Helm Chart
  company: none
  license: MIT
  min_ansible_version: '2.1'

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: [kubernetes, k3s, helm, rancher]

dependencies:
  - role: rmasters270.helm
